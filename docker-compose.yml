services:
  distribution-tracker:
    build: .
    container_name: distribution-tracker
    ports:
      - "3000:3000"
    environment:
      - POLYGON_KEY=${POLYGON_KEY}
      - USERS=${USERS}
      - JWT_SECRET=${JWT_SECRET}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
